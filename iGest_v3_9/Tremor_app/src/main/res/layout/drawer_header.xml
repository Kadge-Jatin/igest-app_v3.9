<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="160dp"
    android:background="@color/brand_sky_blue_light"
    android:paddingStart="0dp"
    android:paddingEnd="0dp"
    android:paddingTop="?attr/actionBarSize"
    android:paddingBottom="8dp"
    android:fitsSystemWindows="true">


    <!-- Top row: Logo + App Name & Tagline -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingTop="24dp"
        android:paddingEnd="16dp">

        <!-- iGest App Logo -->
        <ImageView
            android:id="@+id/appLogoHeader"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/create_logo_rbg"
            android:contentDescription="App Logo" />

        <!-- Space between logo and text -->
        <View
            android:layout_width="12dp"
            android:layout_height="0dp" />

        <!-- App Name and Tagline -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:id="@+id/appName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="iGest"
                android:textStyle="bold"
                android:textSize="18sp"
                android:textColor="@color/brand_dark_blue" />

            <TextView
                android:id="@+id/appTagline"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Tremor Detection"
                android:textSize="14sp"
                android:textColor="@android:color/black" />
        </LinearLayout>
    </LinearLayout>

    <!-- Space between logo+title and username row -->
    <View
        android:layout_width="match_parent"
        android:layout_height="16dp" />

    <!-- Username "button" row -->
    <LinearLayout
        android:id="@+id/headerUserRow"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:clickable="true"
        android:focusable="true"
        android:background="@drawable/header_user_row_background"
        android:foreground="?attr/selectableItemBackground"
        android:gravity="center_vertical"
        android:paddingStart="20dp"
        android:paddingEnd="16dp">

        <!-- User icon (vector asset) -->
        <ImageView
            android:id="@+id/userIcon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_person"
            android:contentDescription="User Icon"
            android:tint="?android:textColorPrimary"
            android:layout_marginEnd="16dp" />

        <!-- Username -->
        <TextView
            android:id="@+id/drawerUsername"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="User"
            android:textSize="16sp"
            android:textColor="?android:textColorPrimary"
            android:textStyle="normal" />
    </LinearLayout>
</LinearLayout>